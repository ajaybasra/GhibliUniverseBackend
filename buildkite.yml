---
steps:
  - label: 'do not panic :cherry_blossom::whale:'
    command: 'echo hello, engineer'
    agents:
      queue: fma-dev

  - label: 'Running tests'
    command: 'docker build -t test --target test .'
    agents:
      queue: fma-dev

  - wait

  - label: 'Building image and pushing to Cloudsmith'
    command: docker build -t docker.myob.com/future-makers-academy/ghibli-universe:latest . && docker push docker.myob.com/future-makers-academy/ghibli-universe:latest
    agents:
      queue: fma-dev